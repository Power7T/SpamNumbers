#!/usr/bin/expect -f
set password "CprT0IzeY4Ww2PgeMII9S"
set host "85.208.51.22"
set user "root"

set timeout 300
spawn ssh -t -o StrictHostKeyChecking=no $user@$host

expect "password:"
send "$password\r"

expect "root@*#"

# STRICT: ONLY this one command
send "openclaw tui\r"

# Wait for TUI to load
sleep 20

# TEST 1: try with phrasing that clearly matches the skill
send "run the spam numbers scrape command\r"
set timeout 120
expect {
    "Starting scrape run" { send_user "\n==== SUCCESS: Scrape started! ====\n" }
    "orchestrator" { send_user "\n==== SUCCESS: Orchestrator! ====\n" }
    "github" { send_user "\n==== SUCCESS: git scraper live! ====\n" }
    "ftc_csv" { send_user "\n==== SUCCESS: FTC running! ====\n" }
}

sleep 30
send "\x03"
expect "root@*#"
send "exit\r"
expect eof
