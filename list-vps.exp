#!/usr/bin/expect -f
set password "CprT0IzeY4Ww2PgeMII9S"
set host "85.208.51.22"
set user "root"

set timeout 300
spawn ssh -o StrictHostKeyChecking=no $user@$host

expect "password:"
send "$password\r"

expect "root@*#"

# 1. LIST ROOT
send "ls -F /root\r"
expect "root@*#"

# 2. LIST SKILLS
send "ls -F /root/.openclaw/workspace/skills\r"
expect "root@*#"

send "exit\r"
expect eof
